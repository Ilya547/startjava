final String TAG = "ExpressCourse";

public void onClick(View view) {
    int x = 3;
    Log.i(TAG, Integer.toString(x));
    x *= x;
    Log.i(TAG, Integer.toString(x));
    x *= x;
    Log.i(TAG, Integer.toString(x));
    x *= x;
    Log.i(TAG, Integer.toString(x));
    x *= x;
    Log.i(TAG, Integer.toString(x));
    x *= x;
    Log.i(TAG, Integer.toString(x));
    x *= x;
    Log.i(TAG, Integer.toString(x));
    x *= x;
    Log.i(TAG, Integer.toString(x));
    x *= x;
    Log.i(TAG, Integer.toString(x));
}